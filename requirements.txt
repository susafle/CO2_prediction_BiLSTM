# BiLSTM CO2 Prediction - Requirements
# Compatible with Mac M1/M2/M3 and systems without AVX support

# Core dependencies - install in this exact order
numpy==1.23.5
pandas==1.5.3
scikit-learn==1.2.2
matplotlib==3.7.1

# TensorFlow installation (choose based on your system):
# For Mac M1/M2/M3 or systems with AVX issues:
tensorflow==2.12.0  # Install with --no-deps flag

# Alternative TensorFlow options if above doesn't work:
# tensorflow-macos==2.12.0  # Mac ARM native (if available)
# tensorflow-cpu>=2.12.0    # CPU-only version for other systems

# Optional: Jupyter notebook support
ipython>=7.0.0
jupyter>=1.0.0

# Installation Instructions:
# ======================
# 
# Step 1: Create virtual environment
# python -m venv bilstm_env
# source bilstm_env/bin/activate  # Linux/Mac
# bilstm_env\Scripts\activate     # Windows
# 
# Step 2: Install TensorFlow carefully (IMPORTANT)
# pip install tensorflow==2.12.0 --no-deps
# 
# Step 3: Install remaining dependencies
# pip install numpy==1.23.5 pandas==1.5.3 scikit-learn==1.2.2 matplotlib==3.7.1
# 
# Step 4: Test installation
# python -c "import tensorflow as tf; print('TensorFlow version:', tf.__version__)"
#
# Troubleshooting:
# - If you get AVX/JAX errors: Use --no-deps flag with tensorflow
# - Mac users: Ensure you're using the correct Python architecture
# - If tensorflow==2.12.0 not available: Try tensorflow>=2.12.0